import { EventLabel } from "../types";
import { COLORS } from "./colors";

export const HOLIDAY_LABEL: EventLabel = {
  id: "holiday_label",
  name: "Holiday",
  color: COLORS[0],
};

export const FIXED_LABELS = [HOLIDAY_LABEL];
